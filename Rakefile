require 'rake'

desc 'Install dependencies'
task :build do |_task|
  puts "==> Building /bin"
  sh "sudo cp ./bin/* /usr/bin/"
  puts "===> builded with sucess now type stickypostrr"
  #sh "bundle exec cucumber -t@one -p html -p pretty -p no_headless -p #{args[:ambiente]}"
end
