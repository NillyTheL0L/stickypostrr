#!/usr/bin/env ruby
require 'glimmer-dsl-libui'
require 'colorize'

include Glimmer

window('StickyPostRR', 400, 200) {
  vertical_box {
    non_wrapping_multiline_entry
  }
  
  on_closing do
    puts '==> Program exited!'.colorize(:red)
  end
}.show
